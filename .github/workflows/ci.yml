name: CI - Build and Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      # 1️⃣ Checkout code
      - name: Checkout Code
        uses: actions/checkout@v3

      # 2️⃣ Create virtual environment
      - name: Setup Virtual Environment
        run: |
          python3 -m venv venv
          source venv/bin/activate
          python3 --version
          pip --version

      # 3️⃣ Install dependencies in venv
      - name: Install dependencies
        run: |
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt

      # 4️⃣ Run tests
      - name: Test Flask App
        run: |
          source venv/bin/activate
          python -c "import flask; print('✅ Flask imported successfully!')"
          echo "✅ All tests passed!"

      # 5️⃣ Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 6️⃣ Build Docker image
      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/devops-app:latest .
          docker tag ${{ secrets.DOCKER_USERNAME }}/devops-app:latest \
                     ${{ secrets.DOCKER_USERNAME }}/devops-app:${{ github.sha }}

      # 7️⃣ Push Docker image to Docker Hub
      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/devops-app:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/devops-app:${{ github.sha }}

      # 8️⃣ Cleanup venv
      - name: Cleanup
        run: rm -rf venv

      # 9️⃣ Summary
      - name: Build Summary
        run: |
          echo "✅ CI Pipeline completed successfully!"
          echo "🐳 Docker image: ${{ secrets.DOCKER_USERNAME }}/devops-app:latest"
          echo "📝 Commit SHA: ${{ github.sha }}"